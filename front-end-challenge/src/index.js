import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import NewServicesPage from './NewServicesPage';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <NewServicesPage />
  </React.StrictMode>
);
